# RunningHub 并发维护说明

修改日期：2026-09-20。沿用仓库 Aladdin Free Public License。

`runninghub-scheduler.mjs` 是共享调度器，普通图片、参考视频和视频测试均从此获取租约。`runninghub-ledger.mjs` 管理跨标签页登记和刷新恢复，`index.js` 保留业务入口。更新时必须一起安装这三个文件及 `html/settings/runninghub.html`。

## 行为

- 单 Key 有效上限取平台额度与单 Key 本地上限的较小值，留空或 0 跟随平台；全局上限留空或 0 不增加额外限制。重复 Key 不增加容量。
- 全局上限覆盖图片、参考视频和生成测试，包含已预留、已提交及状态未知的占用。移除 Key 不会抹掉它尚未结束的占用。
- 上限运行中可调整：调高立即调度，调低不取消在途任务，只停止超出新上限的分配。状态面板区分平台额度、单 Key 有效上限和全局上限。
- 每个租约具有唯一身份，释放只删除自己的记录；刷新状态不清空活动租约。
- Key 的状态探测并行且同 Key 合并请求，分配过程同步检查最新本地容量。
- FIFO 选择当前能执行的任务，受限于忙 Key 的任务不会阻塞其他 Key；按占用比例选择 Key，同负载轮换。
- 远程聚合运行数、排队数仅用于估计外部压力，不重复累加已知本地远程任务；聚合快照存在延迟，服务器容量拒绝仍是最终裁决。
- 图片总期限 15 分钟，参考视频/视频测试 25 分钟；账户查询 15 秒，提交/查询/下载 HTTP 请求默认 60 秒，覆盖响应体传输。原轮询次数限制仍保留。
- 成功、失败、取消终态释放生成槽位；下载与转换不继续占用生成槽位。
- 取消时停止本地等待，并对已知任务 ID 尝试远端取消；取消接口受理不等于远端终态，仍由查询确认释放。接口依据 [RunningHub 官方国际站取消文档](https://www.runninghub.cn/runninghub-api-doc-en/api-425761095)，尚未进行真实账号联调。
- 提交响应丢失时不自动重新生图。已知 taskId 时每 15 秒对账；未知 taskId 时在设置页显示待确认，并提供核对后手动释放的明确操作。
- 图片、参考视频、生成测试的任务查询遇到网络/临时 HTTP 错误时，最多尝试 4 次，以 1/2/4 秒退避；取消立即结束退避，鉴权等明确非临时 4xx 不重试。重试仅用于读取原任务，绝不重试创建请求。耗尽后进入独立对账，不在未知状态下自动清零。
- 独立对账合并重复调用，释放时取消在途查询并清理定时器；迟到的任务 ID 能交给恢复流程，已释放记录不会被迟到回调复活。

## 验证

```powershell
node --check index.js
node --check runninghub-scheduler.mjs
node --check runninghub-ledger.mjs
node --test tests/runninghub-*.test.mjs
```

测试使用模拟网络，包含原始图片入口的调用测试，不消耗平台额度。真实 SillyTavern 界面、国际站 Key 权益和实际工作流仍需联调。重点检查三通道五任务逐个补位、四个独立账号同时运行、取消及慢下载。

## 边界

浏览器需支持 Web Locks、Web Crypto 和 localStorage，使用 HTTPS 或 localhost。HTTP 局域网地址或存储被禁用时会停止新增提交并提示，不静默退回不安全的跨页计数。

同源标签页使用 Web Locks 原子登记通道，localStorage 保存 SHA-256 Key 指纹、任务 ID、状态与结果链接，不额外明文保存 API Key。每页持有浏览器生命周期锁，刷新/关闭后其他页面或重开的页面接管孤立记录。未提交的预约可安全回收；已提交任务只查询，不重新生成。后台标签页变慢不作为“所有者已死”的证据。

设置页支持全局上限、单 Key 上限/暂停、本页队列暂停/恢复和取消等待。等待队列默认最多 100 项，可调整。全局与单 Key 调整写入同源共享策略；排队顺序只保证本页兼容任务 FIFO，不承诺跨页严格 FIFO。

生成终态先保存结果链接再释放通道，媒体下载独立最多 3 路，有界重试不重新生成。设置页可打开结果链接及手动移除记录。链接可能受平台保留时间限制，记录不是文件备份。结果记录不自动删除，界面显示最近 100 条；请定期清理已经保存的记录，存储配额耗尽会阻止新登记。

普通 RunningHub 事件入口及参考视频按钮增加聊天/消息变化保护。原聊天切换、原消息替换或目标失效时不把结果写入当前聊天，结果链接仍可找回；刷新不自动重交未提交队列、不自动插回原消息。多种第三方 iframe 或额外插件的事件监听器仍需在真实酒馆环境验证。

没有远程任务 ID 的未知提交，不能可靠区分“平台接单但响应丢失”和“从未接单”，必须核对平台，不能以超时或刷新推定结束。同账号多个不同 Key 是否共享额度无法仅凭 Key 文本识别，当前只对独立账号池提供容量相加；其他设备/浏览器及共享账号仍依靠平台限额兜底。

同步的 ZIP/格式转换及现有上传准备辅助函数不是所有路径都可被强制中断；生成池不依赖下载/转换完成才能释放。浏览器关闭时不继续执行本地排队任务。以上边界不能用模拟测试替代真实平台验收。

设置页“本地调度状态”显示活动记录与远程 ID。也可用 `window.getRunningHubSchedulerStatus()` 读取脱敏快照。原 `window.resetRunningHubKeyActiveCounts()` 现在只刷新观察状态，不会盲目清零。
